import './SubmitButton.styles.css';

const SubmitButton = () => {
  return (
    <button
      className={'form-submit-button'}
      type={'submit'}
    >
      Submit
    </button>
  );
};

export default SubmitButton;
